fastapi==0.115.0
uvicorn[standard]==0.32.0
websockets==13.1
psutil==7.0.0
GPUtil==1.4.0
docker==7.1.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
aiofiles==24.1.0
# NVIDIA GPU stats via NVML. Works on Python 3.12+ (unlike GPUtil, which needs
# the removed distutils module). Provides the `pynvml` import.
nvidia-ml-py